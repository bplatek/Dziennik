import React from 'react';

import './StickyCards.scss';

export default class StickyCards extends React.Component {
  render() {
    return (
      <div className="page sticky-cards">
        <h2 className="page__title">Notatki</h2>
      </div>
    );
  }
}
